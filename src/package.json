{
  "name": "azotic-topaz",
  "version": "1.0.0",
  "description": "A customizable, virtual workspace.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "clean": "rm profiles && rm workspaces && rm main.json",
    "clean-win": "powershell -Command \"Remove-Item -Recurse -Force profiles, workspaces, main.json\""
  },
  "author": "Gold Team",
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "electron": "^32.1.2"
  },
  "dependencies": {
    "keytar": "^7.9.0"
  }
}
